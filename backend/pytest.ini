[pytest]
asyncio_mode = auto
testpaths = tests
addopts =
    -v
    --tb=short
    --cov=main
    --cov-report=term-missing
    --cov-fail-under=70
filterwarnings =
    ignore::DeprecationWarning
    ignore::pytest.PytestUnraisableExceptionWarning